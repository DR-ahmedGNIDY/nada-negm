import ContactManager from '@/components/dashboard/ContactManager'
import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'رسائل التواصل' }
export default function ContactPage() { return <ContactManager /> }
